{
  "name": "lanseafood-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website with rotating cube and 3D carousel",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "live-server --port=3000 --open=/index.html --watch=js,src --no-css-inject --wait=500",
    "dev": "npm run build && concurrently \"npm run watch\" \"npm run serve\""
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "live-server": "^1.2.2",
    "concurrently": "^8.2.2"
  }
}

